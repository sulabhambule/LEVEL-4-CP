 // static int Kthparent(int node, int k) {
  // for (int i = MAX_LOG - 1; i >= 0; i--) {
  // if (((1 << i) & k) != 0) {
  // node = par[node][i];
  // if (node == 0)
  // return 0;
  // }
  // }
  // return node;
  // }